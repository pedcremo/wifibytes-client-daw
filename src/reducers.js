const initialState = {
    homeJSON: [],
    datosEmpresa:[],
    ratesJSON:[],
    tarifaJSON:[],
    activeRatesJSON:[]
  }
  ​
const rootReducer = (state = initialState, action) => state;

export default rootReducer;